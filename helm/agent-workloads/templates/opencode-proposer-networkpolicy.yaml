{{- if and .Values.opencodeProposer.enabled .Values.opencodeProposer.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "agent-workloads.opencodeProposerName" . }}
  labels:
    {{- include "agent-workloads.opencodeProposerLabels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "agent-workloads.opencodeProposerSelectorLabels" . | nindent 6 }}
  policyTypes:
    - Egress
  egress:
    {{- if .Values.opencodeProposer.networkPolicy.egress.dns.enabled }}
    - to:
        {{- toYaml .Values.opencodeProposer.networkPolicy.egress.dns.destinations | nindent 8 }}
      ports:
        {{- toYaml .Values.opencodeProposer.networkPolicy.egress.dns.ports | nindent 8 }}
    {{- end }}
    {{- range .Values.opencodeProposer.networkPolicy.egress.destinations }}
    - to:
        {{- list (omit . "ports") | toYaml | nindent 8 }}
      {{- with .ports }}
      ports:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- end }}
{{- end }}
