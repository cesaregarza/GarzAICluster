{{- if .Values.monitoring.prometheus.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "garzObservability.fullname" . }}-prometheus
  namespace: {{ include "garzObservability.monitoringNamespace" . }}
  labels:
    {{- include "garzObservability.componentLabels" (dict "component" "prometheus" "root" .) | nindent 4 }}
spec:
  type: {{ .Values.monitoring.prometheus.service.type }}
  ports:
    - name: http
      port: {{ .Values.monitoring.prometheus.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "garzObservability.componentSelectorLabels" (dict "component" "prometheus" "root" .) | nindent 4 }}
{{- end }}
