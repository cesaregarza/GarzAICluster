# Operator-attested test-mode identity; credentials stay in dev SOPS sources.
# Run once per exact image after dev is healthy; retain the durable gate receipt.
stripeSmokeRunner:
  enabled: true
  suspend: true
  expectedAccountId: acct_1TWnMyCn9HWrOqJv
  automation:
    enabled: true
