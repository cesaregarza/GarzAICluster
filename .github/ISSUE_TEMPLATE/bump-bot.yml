name: "Bump a bot image"
description: "Request a digest bump for your bot."
title: "Bump: <choose bot> to <tag or digest>"
labels: ["bump-request"]
body:
  - type: dropdown
    id: bot
    attributes:
      label: Which bot?
      description: "If your bot is missing, mention us to add it to bots.yaml."
      options:
        - agent-8s
        - agent-8s-dev
    validations:
      required: true

  - type: dropdown
    id: mode
    attributes:
      label: How are you specifying the new version?
      options:
        - "tag (easiest)"
        - "digest (advanced)"
        - "pause (scale bot to zero replicas)"
    validations:
      required: true

  - type: input
    id: value
    attributes:
      label: Tag, Digest, or 'pause'
      description: "Examples: v1.2.3  |  sha256:abc... (64 hex)  |  pause (to scale to zero)"
      placeholder: pause
    validations:
      required: true

  - type: textarea
    id: notes
    attributes:
      label: Notes (optional)
      placeholder: "Changelog or release link"
