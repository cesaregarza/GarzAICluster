apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argo-splat-top
  namespace: argocd
spec:
  secretName: argo-splat-top-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: argo.splat.top
  dnsNames:
    - argo.splat.top
